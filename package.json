{
  "name": "assignment-fm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -n \"SERVER,CLIENT\" -c \"blue,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix shared && npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "typecheck": "npm run typecheck --prefix shared && npm run typecheck --prefix server && npm run typecheck --prefix client",
    "test": "tsx --test tests/youtube.test.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@types/babel__traverse": "^7.28.0",
    "@types/d3-scale": "^4.0.9",
    "@types/estree": "^1.0.9",
    "mime-db": "^1.54.0"
  }
}
